import 'package:flutter/material.dart';

class OnlineSessions extends StatelessWidget {
  const OnlineSessions({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
