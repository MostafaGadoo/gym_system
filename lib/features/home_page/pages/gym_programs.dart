import 'package:flutter/material.dart';

class GymPrograms extends StatelessWidget {
  const GymPrograms({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
